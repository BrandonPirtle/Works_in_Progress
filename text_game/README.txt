*** Makefile will be made eventually ***
(this is now being developed within Ubuntu15)

1. termios.h may not be found if running on Windows

2. performed apt-get for sqlite3 and libsqlite3-dev
	a. when compiling c++ files, link sqlite with -lsqlite3 flag